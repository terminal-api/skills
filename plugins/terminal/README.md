# Terminal plugin

This plugin combines Terminal's remote MCP server with portable skills for querying fleet data, configuring the Terminal CLI, and building Terminal integrations.

MCP authentication uses Terminal OAuth and is handled by the agent client. The plugin does not contain or store credentials.

See the repository [README](../../README.md) for installation instructions.
